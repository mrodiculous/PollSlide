# ➜ The Microsoft submission guide has moved

Everything for submitting **PollSlide LIVE** (the in-slide PowerPoint add-in) is in one
folder: **`SUBMIT-TO-MICROSOFT/`** — start with `SUBMIT-TO-MICROSOFT/README.md`.

The sidebar (task-pane) add-in is parked and is not being submitted. The older documents
(`APPSOURCE-SUBMISSION.md`, `RESUBMIT-STEP-BY-STEP.md`) describe that parked add-in and
are kept only for history.
